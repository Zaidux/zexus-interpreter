# Test TX context accessibility in functions
action getTxInfo() {
    return TX.caller;
}

let caller = getTxInfo();
print "TX.caller from function:", caller;

action getTxTimestamp() {
    return TX.timestamp;
}

let ts = getTxTimestamp();
print "TX.timestamp from function:", ts;
