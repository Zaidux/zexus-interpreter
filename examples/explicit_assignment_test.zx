// Explicit assignment semantics (v2 canonical)
let x = 10
let y = 3

x = x + y          // read-modify-write
x += 1             // compound assignment
print("x =", x)    // 14

let m = {"a": 1}
m["b"] = 2
print("map b =", m["b"])
