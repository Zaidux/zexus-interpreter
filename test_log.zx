// Test LOG statement for output redirection

action calculate {
    a = 10;
    b = 20;
    counter = a + b;
    
    log > "calculation.txt";
    print("Result: " + counter);
    print("Addition of " + a + " and " + b);
}

calculate();

print("This should appear on console");
