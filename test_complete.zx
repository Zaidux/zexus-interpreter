print "=== COMPLETE TEST ==="

# Test variables and assignment
let x = 5
let y = 10
print "x = " + string(x) + ", y = " + string(y)

# Test boolean literals and logical operators
if (true && true):
    print "Boolean true works"

if (false || true):
    print "Boolean false and OR work"

# Test comparisons with variables
if (x < y):
    print "x < y works"

if (y >= x):
    print "y >= x works"

# Test function with return values
action add(a, b):
    return a + b

let result = add(x, y)
print "add(x, y) = " + string(result)

print "All tests passed! 🎉"
