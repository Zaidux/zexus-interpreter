print "=== GRAND FINALE TEST ==="
print "🎉 ZEXUS INTERPRETER IS WORKING! 🎉"

# Demonstrate all major features
let numbers = [1, 2, 3, 4, 5]
let sum = 0

for each num in numbers:
    sum = sum + num

print "Sum of numbers: " + string(sum)

action is_even(n):
    return n % 2 == 0

let evens = []
for each num in numbers:
    if (is_even(num)):
        evens = push(evens, num)

print "Even numbers: " + string(evens)

# Test complex boolean logic
let can_drive = true
let has_license = false
let age = 20

if (age >= 18 && can_drive && has_license):
    print "Can drive legally"
else if (age >= 18 && can_drive && !has_license):
    print "Can drive but needs license"
else:
    print "Cannot drive"

print "=== ALL SYSTEMS GO! ==="
print "Zexus interpreter is ready for production! 🚀"
