async action test() {
    return 42;
}

let c = test();
print("Coroutine state: " + c.inspect());

let result = await c;
print("Result: " + string(result));
print("Coroutine after await: " + c.inspect());
