# Simple DI test
print("Testing dependency injection...")

entity Logger {
    name: string
}

entity UserService {
    inject logger: Logger
}

# Register dependency
register_dependency("logger", Logger("MyLogger"))

# Create service
let service = UserService()
print("Service created")
print("Logger: " + string(service.logger))
print("Logger name: " + service.logger.name)
